export type Offer = {
  author: string,
  // authorId: String,
  title: string,
  image: string,
  message: string,
  comment: string,
  type: string,
}

export type User = {
  name: string,
  email: string,
  password: string,
  image: string
}
